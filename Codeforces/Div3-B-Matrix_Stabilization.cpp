// 23.06.24
#include <bits/stdc++.h>
using namespace std;

const int N = 100 + 10;
int t, n, m, a[N][N];

int main()
{
    cin >> t;
    while (t--)
    {
        cin >> n >> m;
        for (int i = 0; i < n; i++)
            for (int j = 0; j < m; j++)
                cin >> a[i][j];

        for (int i = 0; i < n; i++)
            for (int j = 0; j < m; j++)
                int mx = 0;
        for (int k = 0; k < 4; k++)
        {
        }
    }
}